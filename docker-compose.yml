services:
  api-service:
    build: .
    ports:
      - '3000:3000'
    command: sh -c "[ -d node_modules ] || npm install; npx prisma generate; npx prisma migrate deploy; npm run start:dev"

    volumes:
      - .:/app
